<%= form_with(model: veneration, local: true) do |form| %>
  <% if veneration.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(veneration.errors.count, "error") %> prohibited this veneration from being saved:</h2>

      <ul>
      <% veneration.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :caption %>
    <%= form.text_area :caption %>
  </div>

  <div class="field">
    <%= form.label :score %>
    <%= form.number_field :score %>
  </div>

  <div class="field">
    <%= form.label :image %>
    <%= form.file_field :image %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
